# Server Configuration
PORT=4000

# JWT Secret (Change this in production!)
JWT_SECRET=fleetflow_jwt_secret_change_in_production

# Google OAuth Client ID
GOOGLE_CLIENT_ID=your_google_client_id_here

# MongoDB Configuration
# For MongoDB Atlas (Cloud):
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/fleetflow?retryWrites=true&w=majority&appName=Cluster0

# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/fleetflow
